force_eco:
  name: Force Eco Mode
  description: Force the system into eco mode
  fields:
    enable:
      name: Enable
      description: Enable or disable force eco mode
      required: false
      default: true
      selector:
        boolean:

reset_temperatures:
  name: Reset Temperatures
  description: Reset all temperature settings to defaults